{
  "routes": [
    {
      "method": "GET",
      "path": "/documentos",
      "handler": "Documento.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/documentos/count",
      "handler": "Documento.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/documentos/:_id",
      "handler": "Documento.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/documentos",
      "handler": "Documento.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/documentos/:_id",
      "handler": "Documento.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/documentos/:_id",
      "handler": "Documento.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
